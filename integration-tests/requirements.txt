# Integration-test dependencies. Pinned so a run is reproducible on Windows,
# WSL, and the ubuntu-latest GitHub runner. Everything here ships wheels for
# CPython 3.12 - 3.14; pg8000 is a pure-Python PostgreSQL driver, so no
# PostgreSQL client library or compiler is needed.
behave==1.3.3
requests==2.34.2
pg8000==1.31.5
bcrypt==5.0.0
boto3==1.43.67
PyJWT==2.13.0
tenacity==9.1.4
